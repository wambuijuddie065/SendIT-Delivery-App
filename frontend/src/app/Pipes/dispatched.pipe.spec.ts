import { DispatchedPipe } from './dispatched.pipe';

describe('DispatchedPipe', () => {
  it('create an instance', () => {
    const pipe = new DispatchedPipe();
    expect(pipe).toBeTruthy();
  });
});
